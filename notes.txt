for self referencial purposes
express pipeline



http codes (https://www.websitepulse.com/kb/5xx_http_status_codes#)
           (https://www.websitepulse.com/kb/4xx_http_status_codes)
sql (case insensitive)
RETURNING : used to return what ever row has been inserted or modified, it can be used to check if the modification/insertion has occured sucessfully or not

js/sql
<database_name>.prepare("string") : compiles the string into an sql query
.run(parameters) : executes the sql statement where the statement modifies the table, no output
.get(parameters) : executes the sql statement the output is one of the rows of the database
.all() : executes the sql statement and it gives multiple rows as output

sqlite uses TEXT instead of VARCHAR, VARCHAR also works but the limit you apply will not be considered ex: in VARCHAR(10) (10) will be ignored
 
 const res=await fetch('/api/tasks');
 here /api/tasks is not a file path. It is a route (endpoint) exposed by your backend server (localhost). server sends back the the data res in json format for the frontend.